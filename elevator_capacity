number_of_people = int(input())
elevator_capacity = int(input())

elevator_courses = number_of_people // elevator_capacity

if number_of_people % elevator_capacity != 0:
    elevator_courses += 1

print(elevator_courses)
